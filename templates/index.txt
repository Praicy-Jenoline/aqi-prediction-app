<!DOCTYPE html>
<html>

<head>

    <title>Delhi AQI Predictor</title>

    <style>

        body{
            font-family: Arial;
            background: linear-gradient(to right, #74ebd5, #9face6);
            padding:20px;
        }

        .container{
            width:850px;
            margin:auto;
            background:white;
            padding:30px;
            border-radius:15px;
            box-shadow:0px 0px 15px rgba(0,0,0,0.2);
        }

        h1,h2,h3{
            text-align:center;
        }

        form{
            display:grid;
            grid-template-columns:1fr 1fr;
            gap:12px;
            margin-top:20px;
        }

        input{
            padding:12px;
            border-radius:8px;
            border:1px solid #ccc;
        }

        button{
            grid-column:span 2;
            padding:14px;
            background:#4CAF50;
            color:white;
            border:none;
            border-radius:10px;
            font-size:16px;
            cursor:pointer;
        }

        button:hover{
            background:#388E3C;
        }

        .report{
            margin-top:30px;
            background:#f5f5f5;
            padding:25px;
            border-radius:12px;
        }

        .aqi{
            font-size:28px;
            color:#d32f2f;
            font-weight:bold;
        }

        .category{
            font-size:22px;
            color:#1976D2;
            font-weight:bold;
        }

        ul{
            line-height:2;
        }

        .shap-img{
            width:100%;
            margin-top:20px;
            border-radius:12px;
        }

    </style>

</head>

<body>

<div class="container">

    <h1>Delhi AQI Prediction System</h1>

    <form action="/predict" method="POST">

        {% for feature in features %}

            <input
                type="text"
                name="{{ feature }}"
                placeholder="{{ feature }}"
                required
            >

        {% endfor %}

        <button type="submit">
            Predict AQI
        </button>

    </form>

    {% if prediction %}

    <div class="report">

        <h2>Air Quality Report</h2>

        <p class="aqi">
            AQI: {{ prediction }}
        </p>

        <p class="category">
            {{ category }}
        </p>

        <p>
            <strong>Health Advice:</strong>
            {{ advice }}
        </p>

        <hr>

        <h3>Environmental Summary</h3>

        <ul>

            {% for key, value in report.items() %}

                <li>
                    <strong>{{ key }}:</strong>
                    {{ value }}
                </li>

            {% endfor %}

        </ul>

    </div>

    {% if shap_generated %}

        <h2>
            Dynamic SHAP Explanation
        </h2>

        <img
            class="shap-img"
            src="{{ url_for('static', filename='shap_single.png') }}"
        >

    {% endif %}

    {% endif %}

</div>

</body>
</html>